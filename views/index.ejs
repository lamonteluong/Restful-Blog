<% include partials/header %>

<h1>Blogs</h1>

<% blogs.forEach((blog) => { %>
    <h2><%= blog.title %></h2>
    <img src="<%= blog.image %>">
    <p><%= blog.body %></p>
    <h4><%= blog.created %></h4>
<% }) %>


<% include partials/footer %>
